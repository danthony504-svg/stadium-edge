- [Mobile chat SSE stall → infinite spinner](mobile-chat-sse-stall.md) — expo/fetch hangs (not rejects) on dead links; THREE surfaces: stream read, initial connect, AND buildChatContext GETs — all need Promise.race/withTimeout, not just the read.
- [api-server scaling (per-user limits + Redis)](api-server-scaling.md) — Replit edge OVERWRITES X-Forwarded-For (so trust proxy:true reads real, non-spoofable IP); limiters key on Clerk userId else IP; lib/store.ts opt-in Redis via REDIS_URL else in-memory, fall-through on error, atomic Lua window, stable `name` scopes.
- [ParlayBuilder multiple search renders](parlaybuilder-multiple-search-renders.md) — 3 independent homeSearch result blocks (home ~7133, All Sports ~7650, all-upcoming ~8180); a search UX change must hit all of them.
- [Single-game request lock](single-game-request-lock.md) — naming one game game-locks ALL legs incl props to that game; never widen to other games to hit leg count.
- [Adding pick AI signals](adding-pick-ai-signals.md) — ESPN→api-server→client context→SYSTEM_PROMPT 4-hop flow; JSON.stringify auto-serializes new keys; career-vs-season gotcha; honest-null weather; api-server no watcher; ESPN no def-vs-position.
- [Underdog Upset Watch](underdog-upset-watch.md) — upset = mlLean(edge≥1) ON the longer plus-money (≥+100) ML side; card+coach on web+mobile; one source of truth (computeMlLean/buildMlPriceByLabel/detectUpset); mobile flat realOdds price-join gotcha; never invent a dog.
- [ESPN odds fallback](espn-odds-fallback.md) — bulk per-event pickcenter[0] as second-tier real odds when Odds API is down; cache + concurrency notes.
- [Bovada odds fallback](bovada-odds-fallback.md) — third-tier real odds scrape; soccer multi-container + period-label + side-matching gotchas, no UFC.
- [Finals suppression belt-and-braces](finals-suppression-belt-and-braces.md) — render-site and source-side finished-game filtering; 4h cutoff + active-status bypass.
- [Playoff series duplicate labels](playoff-series-duplicate-labels.md) — label-keyed game lookups must prefer future/non-final candidates, not first-match; sort detail lists chronologically.
- [Slip-sweep / fresh-fetch race](slip-sweep-race.md) — per-render re-validator strips just-inserted items; stamp `addedAt` and grace-window them so state can catch up.
- [Upcoming list horizon + build-flow split](upcoming-horizon-and-build-flow.md) — Upcoming must cap how far out it shows (offseason schedules list weeks ahead); upcoming Build asks leg-count first, live Build stays immediate.
- [Team period stats enrichment](team-period-stats.md) — real L10 q1-q4/h1/h2 from ESPN scoreboard linescores; date `dates=` is US-local not UTC; gate to sports with both linescores AND bookmaker period markets.
- [Totals pace mandate + alt swaps](totals-pace-mandate.md) — soft "lean OVER" tiebreaker language gets dropped under scarcity pressure; promote to HARD MANDATE with explicit formula + "drop the leg, don't flip it" out, and always offer alt-total ladder swaps.
- [AI pick safety-net](ai-pick-safety-net.md) — prompt-only HARD BAN enforcement leaks under scarcity; back with post-parse filter (dedup family×period×game + anti-correlated ML/opposite-spread) using token-based side resolution + Unicode-minus normalize + case-insensitive PrizePicks check.
- [Betting-signal data boundary](betting-signal-data-boundary.md) — which advanced factors are prompt-able (edge/EV, key numbers, parlay math, regression) vs. need a feed (line movement, RLM, steam, bet%/money%, CLV, cross-book vig); never prompt-add an unsupported signal — it forces fabrication.
- [Client-side props cache migration](client-side-props-cache-migration.md) — backend market-family additions need a shape-aware staleness check + one-shot `_checked` guard at every fetch gate, or AI sees stale pre-deploy payloads until hard refresh.
- [Empty realProps — two causes](prop-fetch-burst-429.md) — "0 <market> props": PRIMARY=order-blind realProps.slice(400) drops freshly-fetched market props pushed past 400 by cached/other-sport games (tell=realProps:0 but playerHistory:40); fix=float requested-market props to front before slice. SECONDARY=burst-429 cold-cache drop (bounded retry+jitter). Verify via "chat context size" log.
- [Anti-correlated period picks](anti-correlation-period-picks.md) — period parlays from thin pools combine mutually-exclusive legs (Team A 1H ML + Team B 1H -3); separate HARD BAN with enumerated examples + self-check needed.
- [Period-intent enforcement](period-intent-enforcement.md) — period parlays (q1-q4/h1/h2) need 5-layer enforcement: prompt + realProps suffix filter + market-lock REPLACEMENT + fresh-fetch fallback + game-level period markets w/ friendly labels & period-scoped duplicate-family ban.
- [Same-game (SGP) period lever](period-intent-enforcement.md) — "same game"/"sgp" intent APPENDS curated game-level period markets (extend, don't replace) to break the ~8 full-game ceiling; precedence after lockedMarket/periodIntent, skip alts, dedup, totals-bare-number formatting, honesty over padding.
- [Opponent / team-defense route](opponent-defense-route.md) — ESPN per-team feeds have no true "opp allows X" splits; honest scope of /api/sports/team-defense (avgPointsAgainst from record + own-defensive output).
- [System-prompt template-literal escaping](system-prompt-template-literals.md) — chat.ts SYSTEM_PROMPT is a backtick literal; never write `${...}` inside it (gets interpolated and breaks build). Use "<sport>#<teamId>" style placeholders.
- [Parlay — no duplicate market×game combos](parlay-no-duplicate-market.md) — once alts ship, AI stacks main+alt on same side; hard-ban one-per-(game,market-family) + ban alts priced -1000 or worse.
- [Chat intent detection](chat-intent-detection.md) — wantsParlay regex must catch "leg(s)"/"N-leg"; ALSO be conversation-aware: terse follow-ups ("do 15"/"give me the full 15") skip prop-fetch → "only ~10 legs, realProps empty" lie.
- [Bare-signed odds threshold](odds-threshold-bare-signed.md) — "15 leg -300" (signed, no comparator) must infer a per-leg bound from the SIGN: neg→atMost (favorites), pos→atLeast (dogs); unsigned still null. Sync all 3 parseOddsThreshold copies.
- [Odds API alt markets endpoint](odds-api-alt-markets.md) — alternate_spreads/alternate_totals are per-event only; bulk /odds returns 422 and kills mains too.
- [Live props AI pick must be pinned](live-props-ai-pick-pin.md) — multi-book props lists are 200+ rows; per-row highlight gets buried, pin a summary card at the section top.
- [Tracker pick-string forms / CLV matching](tracker-pick-string-forms.md) — tracker persists pick in whatever form the add-path produced (nickname vs full team name); cross-refs vs buildPicksFromOdds must register/normalize both variants, never exact-match.
- [api-server dev is one-shot build+start](api-server-no-watcher.md) — server route edits do not hot-reload; restart the API workflow or it serves stale compiled code + stale caches.
- [Live dead-market guard](live-dead-market-guard.md) — live picks must respect score+clock; HARD-exclude trailing-team ML / out-of-reach spread late; match "Alt Spread" too; feed live score to chat AI.
- [Game-level period markets in context](game-level-period-markets-context.md) — single-game parlays need period legs; odds.ts fetches + prompt documents them but client buildPicksFromOdds must EMIT them (gated to named/period intent); anti-correlation must be period-scoped.
- [Chat SSE heartbeat](chat-sse-heartbeat.md) — gpt-5.4 reasoning model is silent ~30s before 1st token on big context; proxy kills idle SSE → "nothing loaded". Emit ": keep-alive" comment until first token.
- [Multi-key sports + moneyline-only](multi-key-sports-and-moneyline-only.md) — one app sport can fan out to many Odds API keys (soccer/tennis); key per-event fetches by g.sport_key; enforce tennis h2h-only server-side; tennis already wired individual; WNBA PP fallback intentionally omitted.
- [Alt prop-line ladders](alt-prop-ladders.md) — real `*_alternate` rungs fold into the main (player,market) bucket; mains-first ingest + emit, trim band/cap; cushion vs value swap; fresh-fetch fallback must copy `alt`.
- [Confidence badge vs market](confidence-badge-vs-market.md) — "only market picks" complaints come from the client badge (anchored to implied prob), NOT the prompt; surface AI's projected % from EDGE note + edge chip instead.
- [Orientation-flip finished collision](orientation-flip-finished-collision.md) — "Today on a past game" = AI reverses Away@Home, flipped label collides with a different already-final game; fix = prompt exact-orientation rule + render-side gameResolvesToFinal suppression (check raw AND canonical label).
- [Prop-market sync points](prop-market-sync-points.md) — a new prop key must be added in ~6 places (props.ts fetch list, 4+ client label/stat/countable maps, chat.ts lock regex order + directional wording) or it half-works.
- [Value-over-chalk picks](value-over-chalk-picks.md) — "AI just picks the favorite/market" = EV-as-filter + analytics all point at the stronger team; reframe to beat-the-price + required projected-vs-implied gap, with anti-fab escape + safe-ticket precedence.
- [Moneyline winner consistency (mlLean)](moneyline-consistency-mllean.md) — winner flip-flop fix = pure client computeMlLean({side,edge,reasons}) + prompt mandate that must override BOTH variety AND value-over-chalk for the WINNER side only; value-over-chalk picks market/rung, not who.
- [All live-prop players clickable](all-players-clickable-synth.md) — non-pool players open Player Props via a `_synth` player built from REAL prop lines (no invented season stats); simulated 5-game bars OK only under the page's hypothetical disclaimer.
- [Stadium Edge Clerk auth](auth-clerk-stadium-edge.md) — real Clerk replaced a mocked `loggedIn` gate; app stays public (optional auth); routing in App.tsx; DUPLICATE ParlayBuilder in mockup-sandbox is NOT the live app — don't edit it.
- [Mobile auth visual direction](auth-screens-visual-direction.md) — auth screens use BLUE AUTH_ACCENT (not app cyan) by mockup intent — don't revert; signed-out entry is /welcome; decorative hero art may be AI-generated (never-fabricate is data-only).
- [Pre-game venue/streak/season signals](pregame-venue-streak-signals.md) — home/away splits + streak + season record all derive from ESPN schedule already pulled (no new feed); venue orientation gotcha; streak/venue notes are SIDE-markets only; pick validation never checks price/point.
- [Team detail page (clickable teams)](team-detail-page.md) — tap team → player-props-style team-total view; teams aren't the fab exception so stats+chart must be REAL (matchup-history `recent` array); async open needs side+game race guard; small odds multiplier for ~100+ totals.
- [AI Spreads & Totals tab](ai-spreads-totals-tab.md) — a price-only no-vig pick ALWAYS degenerates to "lowest-vig side" (both edges negative w/ vig); drive it from matchup-history L10 margin/pace; home/away match safe because game-string+teamFull+realGameForGame share one odds entry.
- [Player Props add needs skipValidation](player-prop-add-skipvalidation.md) — sample/synth Player Props modal add buttons (AI suggested + Over/Under) must pass `{skipValidation:true}` or filterPicksToReal silently drops the leg; Team Total modal stays validated.
- [Props empty on fallback-odds eventId](props-fallback-eventid-mismatch.md) — when primary Odds API odds 429s, client uses ESPN/Bovada odds whose id 422s the props endpoint → "no props"; fix = pass home/away names, server resolves real Odds API id via free events list.
- [Player stat-lookup name extraction](player-name-extraction.md) — ESPN player-search is STRICT (any extra token→[]); stat-lookup name needs projection/weekday strip + span-search fallback; never blanket-strip will/cam/may (Will Smith), skip them only as standalone fallback candidates. ALSO: period-only asks (no "game by game") now build a canonical statmuse-gamelog query from the resolved name and render real per-period splits.
- [Chat card history blindness](chat-card-history-blindness.md) — stat/period cards have content:"" so the chat AI never saw their data; serialize card payloads into history (histText) so follow-up projection/matchup Qs ground on real on-screen numbers. Chat route is POST /api/chat (not /api/sports/chat).
- [Market-lock starvation backfill](market-lock-starvation-backfill.md) — single-market chat lock ("3 HR hitters") returns 1 pick: client realProps capped ~400 nondeterministic order starves the locked pool; non-period branch needs its own server-side fresh-fetch fallback (MARKETS_BY_SPORT-driven, <5-distinct trigger).
- [StatMuse real-stat source](statmuse-integration.md) — scrape meta-description answer; null-guard generic boilerplate (anti-fabrication); wired into chat statmuseFacts (2.5s budget), stat-lookup card, team-question fallback; ufc/tennis unsupported. Per-game PERIOD breakdowns (e.g. "Q1 points last 5 games") scrape the results TABLE via two-step askStatMuseGameLog (ESPN+meta can't); the same grid now also feeds the AI PICK path (playerPeriodGameLog) so period props ground on real per-game numbers; chat budget is per-fetch not all-or-nothing.
- [Basketball-Reference source (NBA)](bballref-source.md) — real per_game_stats parse for NBA stat card; nba.com/stats blocks cloud IPs (unusable); zero-result search page lists "popular players" → MUST scope link to search-item-name or you return the wrong player (fabrication).
- [Player stat lookup (chat)](player-stat-lookup.md) — deterministic real-ESPN game-log card for ANY player; working search endpoint (NOT mode=prefix), season-aware gamelog, counting-stats-only summary, broad intent gate so misses don't leak to fabricating AI.
- [Live-bets chat intent + live-tag render](live-bets-intent.md) — "give me live bets" gated client(homeLiveGames filter)+server(branch on liveGameCount, NOT odds count); lookupLiveTag must scan all dup labels & prefer in-progress or cards show future tipoff.
- [Focal-game/sport prop float](focal-prop-float.md) — realProps is built in nondeterministic worker order then capped at 400; other sports crowd out the focal game so a single-game prop parlay sees ~11 of 19 players. Float named-game then sport-mention props to front before the cap.
- [Downloadable parlay-slip PNG](slip-download-image.md) — downloadTicketImage renders a dark infographic slip via raw canvas; only REAL data (conf/odds/payout/stake), never fabricated sharp%/line-move/edge; logos must stay LOCAL or toBlob taints; verify via throwaway public/slip-test.html.
- [Chat slip overlay sizing + Add-all silent drop](chat-slip-overlay-and-add-all.md) — measured composerH must drive BOTH chat scroll paddingBottom AND expanded-slip overlay maxHeight (bottom-full grows up, header scrolls off); "+ Add all N" re-validates → lands <N silently, surface "added X of Y".
- [Game-detail AI-pick real-data gate](game-detail-real-data-gate.md) — game-detail picks fall back to PICK_POOL sample (no `real` flag); any AI recommendation surface there must filter `p.real` or it lies about "real prices only".
- [Fix button adds an AI leg](fix-button-ai-leg.md) — Fix silently safens legs AND appends one best REAL leg from realOddsBySport; exclude same-game by orientation-independent pair key (not raw string); total auto-aggregates from parlayLegs.
- [Rate-limiter false-429 pool thinning](rate-limiter-false-429-pool-thinning.md) — "pool too thin" can be the limiter, not the slate; per-IP limiter collapses to one shared proxy IP + was global across all routes, false-429'd free odds fallbacks → sports silently dropped. Caps 30→120, per-route bucket scope, client 429 retry.
- [Thin-slate period unlock](thin-slate-period-unlock.md) — generic "N-leg today" under-fills because periods are gated to named/period intent; unlock when (distinct games + usable prop players) < N, throttle per-game period depth so the 120-cap still spans every game.
- [Big-parlay prop-fetch cap](big-parlay-prop-fetch-cap.md) — "15-leg got ~10 + zero props" = THREE levers: scale JIT prop-fetch caps w/ N; props-first prompt; AND widen game-level breadth (drop alts) but then ADAPTIVELY cap distinct game games to N-reservedProps (reserve=min(4,window-valid players)) or model skips props / under-fills.
- [Add-all slip card finished-game gate](add-all-card-finished-gate.md) — "+ Add all N legs" card vanished while per-leg "+ ADD" stayed; its allOver gate must use gameResolvesToFinal (same as inline cards + onClick), not a status-only check (ESPN Final + stale-future odds time = mismatch).
- [Chat card game time/date label matching](game-time-label-matching.md) — time/date drops off chat pick cards when lookupGameStart/lookupLiveTag use exact-string feed matching; AI label form/orientation differs → use tolerant gameLabelsMatch (expand+lowercase+flip). "Not display X" + screenshot usually = "X is missing, add it".
- [Coach last-meeting takeaways](coach-last-meeting-takeaways.md) — "what did we learn last game/Game 2→3" grounded on REAL ESPN box score of most-recent h2h meeting (matchup-history `lastMeeting`, fail-closed null); honest "most recent meeting" never "Game N"/series unless user says; mobile-only client wiring, web parity gap.
- [Player-vs-opponent matchup history](matchup-history.md) — chat "points vs Team Y" shows REAL prior-meeting lines; opponent resolved EXACT-token+nickname-weight+unique-winner, never substring. PERIOD-vs-opp ("Q1 pts vs Knicks") = separate path: join ESPN dates × StatMuse "against the <opp> game by game" on DATE ±1d (abbrs diverge NY/NYK); parse opp by stopword-bounded tokens (whole-tail grab → null → wrong-team).
- [All-games ticket intent](all-games-intent.md) — "all games today" must be its OWN sizing (one-per-game), NOT inherited N; else inherited count unlocks period padding + nudges N PICK lines while prose says one-per-game (badge≠prose). Gate thinBigParlay + breadthGameCap + server prompt on `allGamesIntent`.
- [MLB batter-vs-pitcher real signal](mlb-batter-vs-pitcher.md) — true BvP career line (banned as fabrication) is real via StatMuse; server-only (reuses mlbPlatoon opposingPitcherName); entity guard MUST be diacritic-insensitive (Díaz); shared deadline pass; NBA positional-defense still unbuildable.
- [Player career-vs-opponent-team signal](player-vs-opponent-career.md) — cross-sport (NBA/NFL/NHL) analog of BvP via StatMuse; opponent name parsed from realGames label (no name fields); guard MUST require first+last (surname-only accepts wrong same-surname player) + skip same-display-name collisions (added athleteId to realProps) + word-boundary opp nickname.
- [Mobile Player Props tab](mobile-props-tab.md) — mirrors web "All Sports"; per-event props fanned out per game (cap 12); register tab in BOTH _layout paths; mobile slip pick-strings MUST match web format (side token always present) for dedupe/AI parity.
- [Mobile Home parlay-assistant redesign](mobile-home-redesign.md) — Featured Players must use REAL props (not web's fake "form X/10"); team abbr via playerTeamId vs game ids; Coach auto-send gated by per-nav `ts` token (not prefill text) + skip-while-streaming.
- [Mobile nav — top hamburger](mobile-nav-hamburger.md) — stadium-mobile dropped bottom Tabs for a Stack + single floating `<NavMenu/>` overlay (top-right hamburger Modal) rendered once in (tabs)/_layout; no drawer dep; trim screens' +96 tab-bar bottom padding.
- [Player Props analytics real-only](player-props-analytics-real-only.md) — mobile suggested-lines + set-your-line hit-rate explorer: derive hit-counts from real game log, show Over/Under odds ONLY at the posted book line (mains-only, no estimate formula); web version fabricates odds — don't copy it.
- [Player-history ambiguous/NaN labels](player-history-ambiguous-labels.md) — mobile Player Props modal: dup-in-labels[] columns (NFL YDS/TD) + NBA "2-5" string FG/3PT must honest-null via an `ambiguous` Set threaded into buildGrid + gameValueForMarket; reset market with useEffect([data]) not render-setState.
- [Chat composer hidden behind keyboard](chat-composer-keyboard.md) — composer is a sibling of KeyboardAwareScrollView (not inside) so keyboard covers it; wrap in KeyboardStickyView w/ offset.opened=insets.bottom; safe on web.
- [Mobile AI Coach stat-card port](mobile-stat-card-port.md) — Expo coach has its OWN live stat-card path (lib/statLookup.ts + PlayerStatCard/PeriodGameLogCard) mirroring web ParlayBuilder; stat-lookup changes must hit BOTH; period boolean broad vs periodPhrase precise; getPlayerHistory takes args object. ALSO mobile-only TEAM card: player-miss→searchTeam→getTeamHistory fallback (web has none).
- [Betting value signals on chat cards](betting-value-signals.md) — no-vig fair line + cross-book +EV (median-consensus vs best price) + football key-numbers + line-shopping (reuse BookCompare); derive ONLY from realOddsBySport both-sides+books, null for props/period; MUST pair spread/total side+opp at the SAME point (market merges multiple points by name|point).
- [Mobile slip never-fabricate matcher](mobile-pickparse-never-fabricate.md) — Expo coach picks must resolve each AI PICK/ALT back to a REAL realOdds entry, fail-closed; emit the entry's odds/market/pick not the AI text.
- [Coach empty-bubble guard](coach-empty-bubble-guard.md) — 200 but blank reply when 0 legs resolve: assistantBubbleText strips from first PICK line so cards+prose both empty; notes appended AFTER PICK lines get stripped too — REPLACE scaffold + backstop, don't append.
- [Mobile coach "couldn't reach feed" = streamChat retry exhaustion](mobile-streamchat-resilience.md) — buildChatContext is fully resilient so the generic build-fail catch ≈ streamChat throwing pre-first-token; retries need backoff + CONNECT_MS must cover body upload on weak uplink; STALL_MS safe at 4s b/c server pings ~every 0.4-0.65s.
- [UFC fight analysis + upset read](ufc-fight-analysis.md) — real ESPN MMA tale-of-tape + data lean + plus-money upset; 0%-stats=placeholder, fighter-id resolve fail-closed, 4 upset-join sites must diacritic-normalize (ESPN name vs odds-feed name); boxing OOS.
- [Alt/period lines on mobile surfaces](alt-lines-mobile-surfaces.md) — feed already carries alt+period markets; display-only gap; never price away from a real rung; pick-string parity w/ buildPicksFromOdds is the dedupe invariant.
- [Mobile AI Coach photo upload (vision)](mobile-chat-image-upload.md) — Expo coach sends up to 3 photos as imageDataUrls→latest-user multimodal turn; validate+dedupe+cap server-side; never-fabricate prompt-only. IMPROVE-MY-SLIP: re-attach last image client-side on improve-intent + server improveFromSlipImage swaps BOTH image addendum AND diversify line to "same games + same leg count".
- [Mobile period markets in chat context](mobile-period-markets.md) — period/2H tickets need an includePeriods emit AND a period-aware slip family (else "Q3 ML"→full-game ML); period markets are live-only pregame.
- [Coach pool spans all sports](coach-pool-all-sports.md) — DEFAULT_SPORTS=SPORTS.map(id) (was 5-sport subset); safe b/c isPickable 48h filter + odds slice(0,12) + 5min cache bound cost, not raw game count; don't re-narrow.
- [Mobile alt lines + alt props port](mobile-alt-lines-props.md) — server already merges alt spreads/totals into markets + flags alt props; mobile-only opt-in (buildRealOdds one rung/side + mains-first prop passes); parser needs no change. Alt-rung pick must be THRESHOLD-AWARE (juiced end for "-300 or less", not even-money) or odds-bound tickets starve.
- [Odds-threshold parlay lock](odds-threshold-parlay.md) — odds-bound tickets ("-300 or less"/"+300 or more") need shared parser×3 + server addendum + client filter; web per-leg cards re-parse independent of messagePicks so gate BOTH paths; odds-cue guard avoids "100 yards" false-pos. ALSO "only N qualify" under-count = model counts MLs only + value-over-chalk vetoes the juiced favs the bound demands; fix = family-count clause (both) + chalk-override (atMost only); data's all there (realOdds uncapped).
- [Clerk Expo v3 password reset](clerk-expo-v3-password-reset.md) — Future API forgot-pw seq: create({identifier})→resetPasswordEmailCode.sendCode/verifyCode/submitPassword→finalize; reset() on exit; skill docs omit it, types redacted.
- [Coach stat-card projection answer](coach-projection-answer.md) — a resolved stat card is the full answer only for pure lookups; projection/opinion asks must ALSO stream a grounded reply built from ONLY the card's real numbers (no parsePicks).
- [Cross-device sync](cross-device-sync.md) — per-user JSON blob sync (savedSlips/tracker) via /api/sync/:ns; read helpers MUST throw on 401 and only mark "synced" after an authenticated 2xx, or a not-ready token clobbers cloud data with empty local.
- [Saved-slip cap + auto-clear](saved-slip-auto-clear.md) — mobile: MAX_SAVED_SLIPS cap in saveCurrentSlip; auto-remove finished slips by matching legs→ESPN feed (nickname+sport, EXACTLY-ONE-candidate fail-closed, every-leg-over); "Save to Photos" not showing = stale Metro bundle, restart expo.
- [Mobile "Save to Photos" slip](slip-download-mobile.md) — Expo Go has no view-shot, so slip PNG is server-rendered (@napi-rs/canvas, must externalize package in build.mjs); never-fabricate the stake/odds defaults; overlay maxHeight floor must never exceed available space or last pick clips.
- [Mobile chat prop-pool breadth](mobile-chat-prop-breadth.md) — Coach "props concentrated in a few games" = order-blind realProps.slice over parallel fetches; deep MLB games fill cap & starve rest. Fix=round-robin balancePropsByGame, not .slice; raise games+cap.
- [Mobile slip AI-pick photos/logos](mobile-slip-ai-pick-photos.md) — real headshots (props) + team logos/abbr (game legs) on slip AI cards; render metadata returned in BuiltChatContext (propPool/gameMeta), NEVER on ChatContext (whole context goes to AI); teamSideFromPick→null for totals; onError→initials.
- [Mobile floating-overlay placement](mobile-floating-overlay-placement.md) — iOS native-stack: a root-level overlay (SlipBar) won't paint over nested tab-stack content; render it per-screen-stack ((tabs)/_layout + each root-stack route + fullScreen Modals), like NavMenu. Stale Metro bundle masquerades as broken code after _layout edits.
- [Coach parlay lead-in suppression](coach-parlay-leadin-suppression.md) — mobile coach must hide the streamed intro prose during a parlay build (show only "Building your parlay…"); driven by isBuildingParlay (build-intent regex on prev user msg OR PICK scaffold) + showBubble !isBuildingParlay.
- [Props-tab AI RECOMMENDED list](props-tab-ai-recommended.md) — sourced from REAL props feed (not chat aiPicks); side = higher-American (value) price, no fabricated edge, yes/no markets force the Yes side; mirror matchProp pick-string; shuffle in useMemo.
- [Slip add-buttons must toggle](slip-add-buttons-toggle.md) — mobile add-to-slip controls (PlayerPropsSheet chips + AI SUGGESTED) must toggle (hasLeg→removeLeg/addLeg, legKey lowercased) AND drop the opposite same-line side; add-only caused "can't unclick" + Over+Under both added.
- [Focal-sport/game realOdds float](focal-odds-float.md) — mobile Coach realOdds.slice over an all-sports pool (MLB iterates first) truncates a lone named game (incl. its Q1 markets) → "no posted realOdds"; fix=float named sport/game to front before cap + raise cap when includePeriods. Same class as focal-prop-float.
- [World Cup soccer props](wc-soccer-props.md) — props ONLY for soccer_fifa_world_cup (goalscorer yes/no line-null, shots, SoT); club leagues honest-empty; multi-key league resolved SERVER-side (no client league param); bare "shots" keyword after specific phrases.
- ["combo" parlay-synonym false-lock](combo-parlay-synonym.md) — chat.ts MARKET_KEYWORDS bare "combo" locked to combo-PROP markets → "10 leg combo" emptied realProps → refusal; guard so leg/parlay-context "combo" doesn't lock, bare "combos"/"combo props" still does.
- [Bare "alt picks" default](alt-picks-bare-request.md) — STRICT: every leg a real alt rung (ML→same-side Alt Spread, drop if none, honest short, never pad); plus-money VALUE + -350 cap; -1000 global ban; ML-convert changes RUNG not side. UNDER-FILL fix: don't stop at one Alt Spread/game — Alt Spread+Alt Total same game are diff families (allowed); reach N cross-game first, then pair+more props; verify slate via /api/sports/odds before trusting "honest short".
- ["+ alt" / "- alt" sign lock](alt-sign-request.md) — force every leg onto plus- or minus-money rungs; game alts steered in bestRungPerSide (only one rung/side so model can't), props via value/cushion, HARD post-parse sign filter is the real guarantee; gated on !oddsThreshold; "-" must be space-anchored so "9-leg alt"≠minus.
- [Push notifications (Expo + api-server)](push-notifications.md) — 4 triggers run from a SECURED cron endpoint (autoscale=no setInterval), Scheduled Deployment hits it; ESPN ids≠OddsAPI ids (join by team), fail-closed match, at-most-once dedup; @workspace/db needs dist .d.ts rebuild after schema add.
- [Mobile bare-alt prop rung selection](mobile-bare-alt-prop-cushion.md) — shared prompt can't pick the rung on mobile (no game-log in context); deterministic matchProp swap via AltRungBias: bare alt→"cushion" (deepest safe rung within -550 floor, -200..-500), value/longshot→"value" (least-aggressive plus-money). Default flipped plus-money→cushion per user.
- [EAS Update (OTA)](eas-update-ota.md) — JS-only OTA via expo-updates; runtimeVersion policy=appVersion ties runtime to expo.version; channels per eas profile; MUST bump version on any native change; pre-expo-updates binaries need one rebuild.
- [Improve-the-ticket chat intent](improve-ticket-intent.md) — improve-slip intent mirrored in coach.tsx wantsImproveSlip + chat.ts improveWording (add new phrasings to BOTH); catches anchor-noun ("better one/ticket"), "make it better", "improve/fix this", AND DO_BETTER_RE ("how can you do better"/"do any better"); "do+better" bypasses the "which is better" comparison exclusion; gated on currentSlip OR prior slip turn.
- [EAS/TestFlight env baking](eas-testflight-env.md) — native EAS builds don't see dev-script env/Replit secrets; bake EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY + EXPO_PUBLIC_DOMAIN (published host, not dev domain) into eas.json `env` or the app crashes on launch / can't reach /api.
- [Coach matchup lookahead horizon](coach-matchup-lookahead-horizon.md) — playoff "what did we learn (game 2→3)" failed b/c isPickable 48h gate drops the next series game (days out, no odds); fix = focal-only horizon widening (now+8d, focalExtra<6/sport) into realGames+historyTargets, odds stay 48h; prompt: matchup reads need NO odds + >48h focal game is analysis-only not pick-eligible; client change → needs OTA.
- [Underdog watch push notification](upset-push-notification.md) — daily model-backed dog push: ports client Upset Watch to server, once/UTC-day KV-cached fan-out, per-user dedupe, fail-closed; price map MUST be sport-namespaced; new pref auto-whitelisted by PREF_KEYS.
- [Coach plain N-leg backfill](coach-generic-leg-backfill.md) — plain N-leg parlay returned short b/c reach-N backstop only ran for alt/period asks; added generic full-game-mains backfill, constraints derived from model's OWN resolved legs (props-only skip, single-game lock, homogeneous-family market-lock), never fabricates.
- [OTA update unsafe (appVersion policy)](ota-update-unsafe-appversion.md) — `eas update` from HEAD crashed the installed iOS app (error boundary on launch); appVersion runtimeVersion (static 1.0.0) doesn't catch native skew + installed build lags HEAD by many commits. Recover via roll-back-to-embedded; ship via NEW native build (eas build/submit), not OTA.
- [Soccer player history (name-keyed)](soccer-player-history.md) — soccer season stats/per-game logs come from StatMuse by NAME (ESPN has no soccer game-log); mobile Player Props gates must unlock on `hasHistorySource = athleteId || (isSoccer && player)`, not athleteId alone.
- [Ticket-size (max legs) cap](ticket-size-cap.md) — max parlay legs enforced in TWO places: chat.ts prompt (governs web+mobile AI) AND mobile MAX_LEGS; web has no numeric cap; restart api-server after prompt change.
- [Coach shows alt prop options](coach-alt-prop-options.md) — every prop PICK appends a prose-only "Alt options:" line (safer cushion + higher-payout value rung, real realProps rungs only); MUST NOT be emitted as PICK lines or they join the slip.
- [Named off-slate player form enrichment](named-player-form-enrichment.md) — mobile Coach injects REAL recent game logs for MULTIPLE named off-pool players (single-name handled by stat card); recentFormOnly flag + prompt note; single-token guard needs isActive+first/last; never fabricates.
- [Conversational HR-evaluation prompt scope](hr-evaluation-conversational.md) — MLB HR analytics rules were build-only, so "do you like these HR targets?" ranked by game total + price instead of real park(Coors)/platoon/recent/BvP; fix = explicit "applies to ANY HR question, not just a build" rule; total is only the run tiebreaker, never the park proxy.
- [Chat reply leaks internal field names](chat-internal-name-leak.md) — SYSTEM_PROMPT documents data by literal field names (realProps/playerHistory/mlbGameEnv…) so the model parrots them to users, esp. on missing-data ("not listed in realProps…"); fix=top-of-prompt "NEVER EXPOSE INTERNAL NAMES" + plain-English subs + OUTPUT-ONLY clause (keep using fields internally); prompt-only, restart api-server.
- [Stat-lookup role-word & matchup hijack guards](stat-lookup-role-word-guard.md) — common-word tokens fuzzy-bind to athletes: role-lead ("pitcher …"→Calvin Pitcher) AND "<stat> in the <team> and <team> game" ("Blue Jays"→Jaydon Blue). parseStatLookup bails; matchup guard MUST be preposition/"between"-anchored or it eats "points and rebounds game log"; mirror web+mobile.
- [Coach stats-first prop discovery](coach-stats-first-discovery.md) — prop/stat DISCOVERY asks (no build word) must show real stats + ask before building, ZERO PICK lines (prompt-only, no client change); reconcile BOTH the props-mandatory block AND the separate "FIND PLAYER PROPS→PICK line" rule or picks leak.
- [Baseball innings period markets](innings-period-markets.md) — F5(ml/runline/total)+1st-inning total are GAME-LEVEL only (no inning player props); mirror quarters/halves; web isPeriodMarket+periodOf/familyOf MUST recognize "F5 "/"1st Inning " or context-slice + dedup skip them.
- [Per-leg projected-edge readout](per-leg-edge-readout.md) — mobile Coach EdgeReadout surfaces (never invents) model%/implied%/edge from the EDGE prose; renders unconditionally, honest "Market price" on miss; sign-excluding regex stops "+14% edge" mislabel.
- [Model Report — real grading + strengths](model-report-grading.md) — grade endpoint fail-closes ungraded; parlay result fail-closed on win side (any ungraded leg→ungraded, a loss is determinative); soccer draw LOSES team ML; strengths advisory-only; gradingRef released in finally.
- [Slip prop teamAbbr can be wrong](slip-prop-teamabbr-wrong.md) — props feed playerTeamId mis-resolves team (Knicks→"NO"→"NO POINTS"); AiPickCard prop subtitle shows AWAY@HOME·MARKET not lone teamAbbr; game legs keep teamAbbr.
- [Server streams resolved prop pool to client](server-returns-resolved-prop-pool.md) — "later games" prop parlay → empty fallback: client propPool is soonest-24-capped so late games' props (model picked them via server backfill) fail-close; fix = SSE {props:[...]} frame merged into propPool before parsePicks; realOdds not capped so only props need it.
- [playerHistory game-log fetch focal float](playerhistory-fetch-focal-float.md) — mobile Coach's per-player log fetch is a THIRD floated+capped pool; MLB-only float starved focal NBA/NFL players past the 40-cap → "no recent log available" lie; float focal game/sport first like realOdds/realProps. Web float differs (unchanged).
- [stadium-mobile unit tests](mobile-unit-tests.md) — no jest/vitest; Node 24 native `node --test 'lib/**/*.test.ts'`; imports need `.ts` ext + allowImportingTsExtensions/noEmit; api.ts can't load (expo/fetch) so extract pure logic to test it.
- [Rich PickCard surfaces](chat-pickcard-rich-look.md) — rich PickCard (rung boxes + stat GRID) on Coach/Props/Slip; only game-detail keeps compact AiPickCard (pills); split via EdgeReadout `grid` prop, real-data-only, "Safety"=relabeled variance.
- [Spread "beat the number" copy must be sign-aware](spread-cover-copy-sign-aware.md) — favourite WINS by the line, underdog only loses-by-fewer (or wins); never phrase a dog cover as "won by X+"; label recent-margin charts as vs varied opponents, NOT ATS vs closing line.
