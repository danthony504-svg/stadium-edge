/** Restore the existing email/password sign-in entry points after App Review. */
export const REQUIRE_AUTH_FOR_APP = true;

/** Keep OTA recovery active, but do not expose update/debug UI to reviewers. */
export const SHOW_OTA_UI_FOR_APP_REVIEW =
  false;
